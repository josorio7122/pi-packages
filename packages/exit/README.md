# @josorio/exit

Pi extension that adds `/exit` as an alias for the built-in `/quit` command.

## Installation

```bash
pi install /path/to/pi-packages/packages/exit
```

## Usage

Type `/exit` in pi to gracefully shut down the session — same behavior as `/quit`.
