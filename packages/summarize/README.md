# @josorio/summarize

Pi extension that adds `/summarize` as an alias for the built-in `/compact` command.

## Installation

```bash
pi install /path/to/pi-packages/packages/summarize
```

## Usage

Type `/summarize` in pi to compact the conversation. Optionally pass custom instructions:

```
/summarize Focus on the auth module changes
```
